####Condition组件
````
1，介绍：主要适用于条件判断；

2，固定属性： 
          createCondition：        函数，返回If和Else组件，spec={value},value是条件值
          If                       value为true时展示子组件
          Else                     value为false时展示子组件
````